<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Aptitune</title>
  <!-- Latest compiled and minified CSS & JS -->
  <link rel="stylesheet" media="screen" href="//netdna.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
  <link rel="stylesheet" type="text/css" href="//cloud.typography.com/7055252/762022/css/fonts.css">
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <script src="//code.jquery.com/jquery.js"></script>
  <script src="//netdna.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Aptitune</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav">
        <% if @current_user && @current_user.email == ENV['MY_FACEBOOK_EMAIL'] && @current_user.provider == 'facebook' %>
        <li class="admin-only"><a href="/users">Users</a></li>
        <li class="admin-only"><a href="/test">Player</a></li>
        <li class="admin-only"><a href="/random_artists">Random artists</a></li>
        <% end %>
        <li><a href="#" data-toggle="modal" data-target="#aboutModal">How to Play</a></li>
        <li><a href="#"><i class="glyphicon glyphicon-stats"></i> Leaderboards</a></li>

      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/team"><i class="fa fa-users"></i> Meet the Team</a></li>
        <% if @current_user %>
        <li><a href="#" data-toggle="modal" data-target="#userModal"><i class="glyphicon glyphicon-dashboard"></i> <%= @current_user.first_name %>'s Dashboard
        </a>
      </li>
      <li><a href="/logout"><i class="fa fa-sign-out"></i> Logout</a></li>
      <% else %>
      <li><a href="#" data-toggle="modal" data-target="#loginModal"><i class="fa fa-sign-in"></i> Login</a></li>
      <li><a href="#" data-toggle="modal" data-target="#signupModal"><i class="fa fa-user-plus"></i> Signup</a></li>
      <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>

<!-- About Modal -->
<%= render "/layouts/about" %>

<%= render "layouts/flash" %>

<%= yield %>

<!-- User Modal -->
<% if @current_user %>
<div class="modal fade" id="userModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">
          <%= @current_user.first_name %>'s Dashboard</h4>
        </div>
        <%= bootstrap_form_for @current_user do |f| %>
        <div class="modal-body">
          <% if @current_user && @current_user.email == ENV['MY_FACEBOOK_EMAIL'] && @current_user.provider == 'facebook' %>
          <ul>
            <li class="admin-only">email: <%= @current_user.email %></li>
            <li class="admin-only">provider: <%= @current_user.provider %></li>
          </ul>
          <% end %>

          <div role="tabpanel">
            <!-- Nav tabs -->
            <ul class="nav nav-tabs dashboard-tabs" role="tablist">
              <li role="presentation" class="active">
                <a href="#genres" aria-controls="genres" role="tab" data-toggle="tab">
                  <i class="fa fa-headphones">&nbsp;&nbsp;</i>Update Genres
                </a>
              </li>
              <li role="presentation">
                <a href="#profile" aria-controls="profile" role="tab" data-toggle="tab"><i class="fa fa-user">&nbsp;&nbsp;</i>Update Profile
                </a>
              </li>
            </ul>
            <!-- Tab panes -->
            <div class="tab-content">
              <div role="tabpanel" class="tab-pane fade in active well well-lg" id="genres">
                <% if @genres %>
                <%= f.collection_check_boxes :genre_ids, @genres, :id, :name, hide_label: true %>
                <% end %>
              </div>
              <div role="tabpanel" class="tab-pane fade well well-lg" id="profile">
                <%= f.text_field :first_name, placeholder: "First name", icon: "user" %>
                <%= f.text_field :last_name, placeholder: "Last name", icon: "user" %>
                <%= f.text_field :email, placeholder: "Email", icon: "envelope" %>
                <%= f.password_field :password, placeholder: "Password", icon: "lock" %>
              </div>
            </div>

          </div>




        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
          <%= f.primary %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <% else %>
  <!-- login modal -->
  <div class="modal fade" id="loginModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="myModalLabel">Login</h4>
        </div>
        <div class="modal-body">

          <div class="list-group">
            <a class="list-group-item" href="/auth/facebook">
              <i class="fa fa-facebook fa-fw"></i>&nbsp; Login with Facebook
            </a>
            <a class="list-group-item" href="/auth/google_oauth2">
              <i class="fa fa-google fa-fw"></i>&nbsp; Login with Google
            </a>
            <a class="list-group-item" href="/auth/twitter">
              <i class="fa fa-twitter fa-fw"></i>&nbsp; Login with Twitter
            </a>
            <a class="list-group-item" href="/auth/linkedin">
              <i class="fa fa-linkedin fa-fw"></i>&nbsp; Login with LinkedIn
            </a>
          </div>

          <%= bootstrap_form_tag url: '/login' do |f| %>
          <%= f.text_field :email, placeholder: "Email", icon: "user", hide_label: true %>
          <%= f.password_field :password, placeholder: "Password", icon: "lock", hide_label: true %>

        </div>
        <div class="modal-footer">
          <%= link_to "Forgot password?", new_password_path, class: "btn btn-default" %>
          <%= f.submit "Login", class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <!-- signup modal -->
  <div class="modal fade" id="signupModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title" id="myModalLabel">Signup</h4>
        </div>
        <div class="modal-body">

          <div class="list-group">
            <a class="list-group-item" href="/auth/facebook">
              <i class="fa fa-facebook fa-fw"></i>&nbsp; Signup with Facebook
            </a>
            <a class="list-group-item" href="/auth/google_oauth2">
              <i class="fa fa-google fa-fw"></i>&nbsp; Signup with Google
            </a>
            <a class="list-group-item" href="/auth/twitter">
              <i class="fa fa-twitter fa-fw"></i>&nbsp; Signup with Twitter
            </a>
            <a class="list-group-item" href="/auth/linkedin">
              <i class="fa fa-linkedin fa-fw"></i>&nbsp; Signup with LinkedIn
            </a>
          </div>

          <% @user = User.new %>
          <%= bootstrap_form_for @user do |f| %>
          <%= f.text_field :first_name, placeholder: "First name", icon: "user", hide_label: true %>
          <%= f.text_field :last_name, placeholder: "Last name", icon: "user", hide_label: true %>
          <%= f.text_field :email, placeholder: "Email", icon: "envelope", hide_label: true %>
          <%= f.password_field :password, placeholder: "Password", icon: "lock", hide_label: true %>

        </div>
        <div class="modal-footer">
          <%= f.primary %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <% end %>
</body>
</html>